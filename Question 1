# include <stdio.h>
int main(){
    float a,b;
    printf("enter the amount for 1 and 2 respectively ");
    scanf("%f %f",&a,&b);
    // here we are going here to print the value of sum
    printf("the sum is %f",a+b);
    return 0;
}
