#include <stdio.h>
#include <stdlib.h>
int main(){
   int n;
   int rev=0;
   printf("enter a number ");
   scanf("%d",&n);
   // condition for revrsing a number 
   while(n>0){
      rev=rev*10+(n%10);
      n/=10;
   }
   printf("%d is the reverse of the number\n",rev);
  
      return 0;
}
